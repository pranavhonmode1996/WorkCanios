import React from 'react';
import {Grid} from '@material-ui/core';
import SearchBar from '../searchBar';

const Home = () => {
    return(
        <Grid container>
            <SearchBar />
        </Grid>
    )
}

export default Home;